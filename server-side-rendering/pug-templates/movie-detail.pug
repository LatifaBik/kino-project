extends layout/main

block content
  h1.movie-detail The movie detail

  a.movie-back-link(href="/movies") ← Back to movie site

  article.movie
    h1= movie.title

    if movie.intro
      .intro= movie.intro

    if movie.image
      img(src=movie.image.url)
